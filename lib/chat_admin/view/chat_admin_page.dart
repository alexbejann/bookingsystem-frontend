
import 'package:flutter/material.dart';

class ChatAdmin extends StatelessWidget {
  const ChatAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chat Admin'),);
  }
}
